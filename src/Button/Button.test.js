import { Button } from '.';

describe('<Button />', () => {
  it('is truthy', () => {
    expect(Button).toBeTruthy();
  });

  it('is still truthy', () => {
    expect(Button).toBeTruthy();
  });
});
