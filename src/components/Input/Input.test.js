// import React from 'react';
// import { render } from '@testing-library/react';
// import Input from '.';

describe('<Input />', () => {
  it('runs', () => {
    expect(true).toBeTruthy();
  });

  // it('renders an <input>', () => {
  //   const { getByPlaceholderText } = render(<Input placeholder='test' />);
  //   expect(getByPlaceholderText('test')).toBeInTheDocument();
  // });

  // it('renders a <label> with given `label` prop', () => {
  //   const { getByLabelText } = render(<Input id='test-input' label='test' />);
  //   expect(getByLabelText('test')).not.toBeNull();
  // });

  // describe('with the `label` prop', () => {
  //   it('renders a <label> element successfully', () => {
  //     const testLabel = 'First name';
  //     const wrapper = shallow(<Input label={testLabel} />);
  //     const label = wrapper.find('label');
  //     expect(label).toHaveLength(1);
  //     expect(label.text()).toBe(testLabel);
  //   });
  // });
});
